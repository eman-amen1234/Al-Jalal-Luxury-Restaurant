document.addEventListener("DOMContentLoaded", function () { // 1. تأثير تغيير الـ Navbar عند تمرير الصفحة لأسفل (Scroll Effect) const header = document.getElementById("mainHeader"); window.addEventListener("scroll", function () { if (window.scrollY > 50) { header.classList.add("scrolled"); } else { header.classList.remove("scrolled"); } }); // 2. تفعيل نظام الفلترة والتنقل لقائمة الطعام (Menu Tabs System) const tabButtons = document.querySelectorAll(".tab-btn"); const menuCards = document.querySelectorAll(".menu-card"); tabButtons.forEach((button) => { button.addEventListener("click", function () { // إزالة الكلاس الفعال من كل الأزرار وإضافته للزر المضغوط tabButtons.forEach((btn) => btn.classList.remove("active")); this.classList.add("active"); const filterValue = this.getAttribute("data-filter"); // تصفية أطباق القائمة menuCards.forEach((card) => { if (filterValue === "all") { card.style.display = "block"; // إضافة تأثير ظهور خفيف card.style.animation = "fadeIn 0.5s ease forwards"; } else { if (card.getAttribute("data-category") === filterValue) { card.style.display = "block"; card.style.animation = "fadeIn 0.5s ease forwards"; } else { card.style.display = "none"; } } }); }); }); // 3. تأكيد الحجز (تأثير وهمي عند إرسال الفورم للجمالية) const bookingForm = document.querySelector(".booking-form-container form"); if (bookingForm) { bookingForm.addEventListener("submit", function (e) { e.preventDefault(); const clientName = document.getElementById("name").value; alert( `شكراً لك يا ${clientName}، تم استلام طلب حجز الطاولة الخاص بك بنجاح! سنتصل بك قريباً لتأكيد الحجز.` ); bookingForm.reset(); }); } });